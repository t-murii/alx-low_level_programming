#include "main.h"

/**
 * _abs - returns absolute value of an integer
 * @n: the variable to check
 * Return: the absolute value of n
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);

	}
	return (-n);
}
