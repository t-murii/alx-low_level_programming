#include "main.h"

/**
 * _strlen - returns the length of the string
 *
 * @s: char type pointer
 * count: int used to store length of string
 *
 * Return: length of string
 */

int _strlen(char *s)
{
	int count;

	for (count = 0; s[count] != '\0'; count++)
		continue;

	return (c);
}
